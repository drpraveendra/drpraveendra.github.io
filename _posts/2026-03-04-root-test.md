---
layout: post
title: "Root Test (Cauchy's Root Test) – Complete Guide with Examples"
date: 2026-03-04
description: "Learn Cauchy's Root Test for series convergence with full statement, comparison with Ratio Test, and solved examples involving nth powers."
---

The **Root Test** (also called **Cauchy's Root Test**) is ideal for series where the general term is raised to the **power of $n$**.

---

## Statement of the Test

Let $\sum a_n$ be a series of non-negative terms. Compute:

$$
L = \lim_{n \to \infty} \sqrt[n]{a_n} = \lim_{n\to\infty} (a_n)^{1/n}
$$

- If $L < 1$ → series **converges absolutely**
- If $L > 1$ → series **diverges**
- If $L = 1$ → test is **inconclusive**

---

## When to Use It

Use the Root Test when $a_n$ has the form:
- $a_n = \left(f(n)\right)^n$
- $a_n = \left(\frac{p(n)}{q(n)}\right)^n$
- Any expression where taking the $n$-th root simplifies nicely

---

## Solved Example 1

**Test:** $\displaystyle\sum_{n=1}^{\infty} \left(\frac{2n}{3n+1}\right)^n$

$$
(a_n)^{1/n} = \frac{2n}{3n+1}
$$

$$
L = \lim_{n\to\infty} \frac{2n}{3n+1} = \frac{2}{3}
$$

Since $L = \frac{2}{3} < 1$, the series **converges**. ✓

---

## Solved Example 2

**Test:** $\displaystyle\sum_{n=1}^{\infty} \left(\frac{n}{n+1}\right)^{n^2}$

$$
(a_n)^{1/n} = \left(\frac{n}{n+1}\right)^n = \left(1 - \frac{1}{n+1}\right)^n
$$

$$
L = \lim_{n\to\infty} \left(1 - \frac{1}{n+1}\right)^n = e^{-1} = \frac{1}{e} < 1
$$

Since $L < 1$, the series **converges**. ✓

---

## Solved Example 3

**Test:** $\displaystyle\sum_{n=1}^{\infty} \frac{1}{(\ln n)^n}$ for $n \geq 2$

$$
(a_n)^{1/n} = \frac{1}{\ln n}
$$

$$
L = \lim_{n\to\infty} \frac{1}{\ln n} = 0
$$

Since $L = 0 < 1$, the series **converges**. ✓

---

## Solved Example 4 — Divergence

**Test:** $\displaystyle\sum_{n=1}^{\infty} \left(\frac{3n+1}{2n}\right)^n$

$$
(a_n)^{1/n} = \frac{3n+1}{2n}
$$

$$
L = \lim_{n\to\infty} \frac{3n+1}{2n} = \frac{3}{2}
$$

Since $L = \frac{3}{2} > 1$, the series **diverges**. ✗

---

## Root Test vs Ratio Test

| Feature | Root Test | Ratio Test |
|---------|-----------|------------|
| Best for | Terms of form $(f(n))^n$ | Factorials, exponentials |
| Same conclusion | Yes, when both work | Yes |
| Inconclusive | $L = 1$ | $L = 1$ |

**Note:** Whenever the Ratio Test works, the Root Test gives the same result — but Root Test is more powerful in some cases.

---

## Useful Limit

$$
\lim_{n\to\infty} n^{1/n} = 1 \qquad \lim_{n\to\infty} (c)^{1/n} = 1 \text{ for any constant } c > 0
$$

These are frequently needed when simplifying Root Test calculations.
